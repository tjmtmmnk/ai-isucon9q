# isuconソルバー

## 目標

- isuconのルールに則って可能な限りスコアを高くする
- MackerelのMCPサーバを利用して得られる情報から、isuconのスコアを高くするための解決策を提案・実装する

## 作業記録

- isucon-solver-log.md に記録を残す
  - どういった情報をもとに、どのような改善案を考えたか 
  - どのように発見したか（ツール名だけでなく、具体的に何を観測してボトルネックと判断したかを記録する）
    - (例) 「Mackerel HTTP Server Stats で GET /users/transactions.json の P95 が 5668ms と最も遅かった」
    - (例) 「Mackerel DB Query Stats で SELECT * FROM categories WHERE id = ? が 105,857回実行されていた」
    - (例) 「Mackerel Host Metrics で CPU 使用率が 95% に達していた」
  - 実装した改善案の内容
  - ベンチマークのスコアの変化
- 変更を加える単位でコミットする

## ルール

- 振る舞いを変えてはいけない
- bench/ 以下のコードを読み込むことを禁止する
- 外部サービスを利用してはいけない
  - 例えばCDNやクラウドサービスなどを利用してはいけない
  - しかし、データベースを変更したり、ミドルウェアを追加したりすることは許可される
- 再起動してもベンチマークが通ること
- 最大3台のサーバを利用できる
  - 初期状態はnginx, app, dbの3コンテナを利用しているので、すでに3台を使っていることになる

## インフラ
- docker上で動作する
- Mackerelの hostId は `5Cu3Vrp85x3`

## 変更対象
- アプリケーション: webapp/go ディレクトリ以下
- sql: webapp/sql ディレクトリ以下
- ミドルウェア設定: webapp/docker-compose.yml および webapp/etc ディレクトリ以下

## 変更の反映
- `cd webapp && docker compose up --build` で変更を反映する

## アルゴリズム

1. 作業を始める前に isucon-solver-log.md の記録を確認し、過去の改善案やスコアの変化を把握する
2. ベンチマークを動かして、現在のスコアを取得する
   - 整合性チェックに失敗して、スコアが0点になる場合は、まず整合性チェックを通過することを目指す
3. 現在のスコアを改善するためのボトルネックを特定する
   - MackerelのMCPサーバから、HTTPサーバ統計情報・データベース統計情報を取得する
     - 必要に応じてMackerelのトレース検索やトレース詳細も確認する 
     - Rate Limit Exceeded エラーが出た場合は、しばらく時間を置いてから再度アクセスする
   - CPU使用率、メモリ使用率、ディスクI/O、ネットワークI/Oなどのリソース使用状況を確認する
   - @docs/manual.md を読み、どの変更がスコア向上に大きく影響するかを把握する
4. ボトルネックを解消するための具体的な対策を検討・実装する
   - 例えば、データベースのインデックス追加、クエリの最適化、キャッシュの導入、ミドルウェアの設定変更など
   - 重要: 改善案が複数あっても、一度に複数の変更を加えないこと。1つずつ変更を加えて、振る舞いが変わらないことを確認しながら進めること。
5. ベンチマークを1度だけ動かす。そして、スコアの改善を確認する。
6. スコアが改善されていれば、その対策を維持し、さらなる改善策を検討する
7. スコアが改善されていなければ、別の対策を検討・実装し、再度ベンチマークを動かす
8. ボトルネックを特定するために他に必要な情報がある場合はフィードバックする
9. これを繰り返し、可能な限りスコアを高くする
